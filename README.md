# FORM / STANDARD

Minimalist essentials brand with neutral hierarchy, generous whitespace, product-first information design, and polished UX.

Open `index.html` or run a local HTTP server. Cloudflare Pages root output with no build command. Not deployed.